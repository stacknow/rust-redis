[package]
name = "rust-redis-app"
version = "0.1.0"
edition = "2021"

[dependencies]
actix-web = "4"
actix-rt = "2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
redis = "0.23"  # Redis client for Rust
dotenv = "0.15"  # To load environment variables
